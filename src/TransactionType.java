/**
 * The TransactionType enum defines the types of transactions that can be performed on a bank account.
 * It includes two types: DEPOSIT and WITHDRAWAL.
 */
public enum TransactionType {
    DEPOSIT, WITHDRAWAL
}
